{
  "name": "gaishi-hackathon-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "format:check": "pnpm -r format:check",
    "graphql-codegen": "pnpm -r graphql-codegen",
    "prepare": "husky"
  },
  "packageManager": "pnpm@9.12.3",
  "dependencies": {
    "graphql-config": "^5.1.3"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10"
  }
}
